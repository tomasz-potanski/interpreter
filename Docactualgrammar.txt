The Language actualgrammar
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of actualgrammar==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in actualgrammar are the following:
  | ``Array`` | ``Boolean`` | ``Char`` | ``Double``
  | ``False`` | ``Function`` | ``Integer`` | ``String``
  | ``True`` | ``Void`` | ``begin`` | ``const``
  | ``do`` | ``elif`` | ``else`` | ``end``
  | ``endif`` | ``for`` | ``function`` | ``if``
  | ``intToStr`` | ``of`` | ``print`` | ``proc``
  | ``procedure`` | ``program`` | ``return`` | ``strToInt``
  | ``then`` | ``to`` | ``var`` | ``while``

The symbols used in actualgrammar are the following:
  | . | ; | ( | )
  | : | , | = | :=
  | [ | ] | *= | /=
  | += | -= | ++ | --
  | <- | <-- | + | -
  | * | / | || | &&
  | .equals | < | <= | >
  | >= | == | != | ..

===Comments===
Single-line comments begin with //, #.Multiple-line comments are  enclosed with /* and */, { and }, {* and *}.

==The syntactic structure of actualgrammar==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | //ProgramNameHeader// //Block// ``.`` 
  | //ProgramNameHeader// | -> | ``program`` //Ident// ``;`` 
  |  |  **|**  | **eps**
  | //Block// | -> | //VariableDeclaration// //ProcDeclaration// //Stmt2// 
  | //[Stmt]// | -> | **eps**
  |  |  **|**  | //Stmt// //[Stmt]// 
  | //ProcDeclaration// | -> | ``proc`` //[ProcDeclLine]// 
  |  |  **|**  | ``proc`` //[ProcDeclLine]// //[FuncDeclLine]// 
  |  |  **|**  | ``proc`` //[FuncDeclLine]// 
  |  |  **|**  | **eps**
  | //ProcDeclLine// | -> | ``procedure`` //Ident// ``(`` ``)`` ``;`` //VariableDeclaration// //Stmt2// 
  |  |  **|**  | ``procedure`` //Ident// ``(`` //VarDeclarationLine// ``)`` ``;`` //VariableDeclaration// //Stmt2// 
  | //[ProcDeclLine]// | -> | **eps**
  |  |  **|**  | //ProcDeclLine// //[ProcDeclLine]// 
  | //FuncDeclLine// | -> | ``function`` //Ident// ``(`` ``)`` ``:`` //Type// ``;`` //VariableDeclaration// //Stmt2// 
  |  |  **|**  | ``function`` //Ident// ``(`` //VarDeclarationLine// ``)`` ``:`` //Type// ``;`` //VariableDeclaration// //Stmt2// 
  | //[FuncDeclLine]// | -> | **eps**
  |  |  **|**  | //FuncDeclLine// //[FuncDeclLine]// 
  | //FuncArg// | -> | //VarDeclarationLine// 
  |  |  **|**  | **eps**
  | //VariableDeclaration// | -> | ``var`` //[VarDeclarationLine]// 
  |  |  **|**  | **eps**
  | //VarDeclarationLine// | -> | //[Ident]// ``:`` //Type// ``;`` 
  | //[VarDeclarationLine]// | -> | //VarDeclarationLine// 
  |  |  **|**  | //VarDeclarationLine// //[VarDeclarationLine]// 
  | //[Ident]// | -> | //Ident// 
  |  |  **|**  | //Ident// ``,`` //[Ident]// 
  | //ConstantDeclaration// | -> | **eps**
  |  |  **|**  | ``const`` //[ConstDeclLine]// 
  | //ConstDeclLine// | -> | //Ident// ``=`` //LiteralValue// ``;`` 
  | //LiteralValue// | -> | //Integer// 
  | //[ConstDeclLine]// | -> | //ConstDeclLine// 
  |  |  **|**  | //ConstDeclLine// //[ConstDeclLine]// 
  | //BoolLit// | -> | ``True`` 
  |  |  **|**  | ``False`` 
  | //Stmt// | -> | ``;`` 
  |  |  **|**  | //Ident// ``:=`` //Ident// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``:=`` //Ident// ``;`` 
  |  |  **|**  | //Ident// ``:=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``:=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``:=`` //BExp// ``;`` 
  |  |  **|**  | //Ident// ``:=`` //BoolLit// ``;`` 
  |  |  **|**  | ``return`` //Integer// ``;`` ``;`` 
  |  |  **|**  | //Ident// ``:=`` //String// ``;`` 
  |  |  **|**  | //Ident// ``:=`` ``strToInt`` //String// ``;`` 
  |  |  **|**  | //Ident// ``:=`` ``intToStr`` //Integer// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``:=`` //BExp// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``:=`` //BoolLit// ``;`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``:=`` //String// ``;`` 
  |  |  **|**  | //Ident// ``*=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``/=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``+=`` //Exp// ``;`` 
  |  |  **|**  | //Ident// ``-=`` //Exp// ``;`` 
  |  |  **|**  | //IfStmt// 
  |  |  **|**  | ``while`` //BExp// ``do`` //Stmt// 
  |  |  **|**  | ``for`` //Ident// ``:=`` //Exp// ``to`` //Exp// ``do`` //Stmt// 
  |  |  **|**  | ``print`` ``<-`` ``(`` //String// ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<-`` ``(`` //BoolLit// ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``[`` //Integer// ``]`` ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<-`` ``(`` //Exp// ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<-`` ``(`` //BExp// ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<-`` ``(`` //Char// ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``(`` ``)`` ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``(`` //Exp// ``)`` ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``(`` //BExp// ``)`` ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``(`` //String// ``)`` ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``(`` //Ident// ``)`` ``)`` ``;`` 
  |  |  **|**  | ``print`` ``<--`` ``(`` //Ident// ``(`` //Ident// ``[`` //Integer// ``]`` ``)`` ``)`` ``;`` 
  |  |  **|**  | //Ident// ``:=`` //Procc// 
  |  |  **|**  | //Ident// ``(`` ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``[`` //Integer// ``]`` ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //Exp// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //BExp// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //String// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //FuncSyg// ``)`` ``;`` 
  |  |  **|**  | //Stmt1// 
  | //Stmt2// | -> | ``begin`` //[Stmt]// ``end`` 
  |  |  **|**  | ``(`` //Stmt// ``)`` 
  | //Stmt1// | -> | ``++`` //Ident// ``;`` 
  |  |  **|**  | ``--`` //Ident// ``;`` 
  |  |  **|**  | //Stmt2// 
  | //IfStmt// | -> | ``if`` //BExp// ``then`` //Stmt// ``endif`` 
  |  |  **|**  | //IfStmt1// 
  | //IfStmt1// | -> | ``if`` //BExp// ``then`` //Stmt// ``else`` //Stmt// ``endif`` 
  |  |  **|**  | //IfStmt2// 
  | //IfStmt2// | -> | ``if`` //BExp// ``then`` //Stmt// ``elif`` //BExp// ``then`` //Stmt// ``endif`` 
  |  |  **|**  | //IfStmt3// 
  | //IfStmt3// | -> | ``if`` //BExp// ``then`` //Stmt// ``elif`` //BExp// ``then`` //Stmt// ``else`` //Stmt// ``endif`` 
  |  |  **|**  | ``(`` //IfStmt// ``)`` 
  | //Procc// | -> | //Ident// ``(`` ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``[`` //Integer// ``]`` ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //Exp// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //BExp// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //String// ``)`` ``;`` 
  |  |  **|**  | //Ident// ``(`` //FuncSyg// ``)`` ``;`` 
  | //Exp// | -> | //Exp// ``+`` //Exp1// 
  |  |  **|**  | //Exp// ``-`` //Exp1// 
  |  |  **|**  | //Exp1// 
  | //Exp1// | -> | //Exp1// ``*`` //Exp2// 
  |  |  **|**  | //Exp1// ``/`` //Exp2// 
  |  |  **|**  | //Exp2// 
  | //Exp2// | -> | //Integer// 
  |  |  **|**  | //Ident// 
  |  |  **|**  | //Ident// ``(`` ``)`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``)`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``[`` //Integer// ``]`` ``)`` 
  |  |  **|**  | //Ident// ``(`` //Ident// ``)`` 
  |  |  **|**  | //Ident// ``(`` //Exp// ``)`` 
  |  |  **|**  | //Ident// ``(`` //BExp// ``)`` 
  |  |  **|**  | //Ident// ``(`` //String// ``)`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` 
  |  |  **|**  | ``(`` //Exp// ``)`` 
  | //BExp// | -> | //BExp// ``||`` //BExp1// 
  |  |  **|**  | //Exp// 
  |  |  **|**  | //BExp1// 
  | //BExp1// | -> | //BExp1// ``&&`` //BExp2// 
  |  |  **|**  | //BExp2// 
  | //BExp2// | -> | //Exp// //RelOp// //Exp// 
  |  |  **|**  | //Ident// ``.equals`` ``(`` //String// ``)`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``.equals`` ``(`` //String// ``)`` 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` ``.equals`` ``(`` //Ident// ``)`` 
  |  |  **|**  | //String// ``.equals`` ``(`` //Ident// ``)`` 
  |  |  **|**  | //Ident// ``.equals`` ``(`` //Ident// ``)`` 
  |  |  **|**  | //String// ``.equals`` ``(`` //String// ``)`` 
  |  |  **|**  | //BoolLit// 
  |  |  **|**  | //Ident// 
  |  |  **|**  | //Ident// ``[`` //Integer// ``]`` 
  |  |  **|**  | ``(`` //BExp// ``)`` 
  | //RelOp// | -> | ``<`` 
  |  |  **|**  | ``<=`` 
  |  |  **|**  | ``>`` 
  |  |  **|**  | ``>=`` 
  |  |  **|**  | ``==`` 
  |  |  **|**  | ``!=`` 
  | //Type// | -> | ``Void`` 
  |  |  **|**  | ``Integer`` 
  |  |  **|**  | ``Boolean`` 
  |  |  **|**  | ``String`` 
  |  |  **|**  | ``Char`` 
  |  |  **|**  | ``Double`` 
  |  |  **|**  | ``Function`` ``(`` //Type// ``)`` ``:`` //Type// 
  |  |  **|**  | //Type1// 
  | //Type2// | -> | ``Array`` ``[`` //Integer// ``..`` //Integer// ``]`` ``of`` //Type// 
  |  |  **|**  | ``(`` //Type// ``)`` 
  | //FuncSyg// | -> | ``Function`` ``(`` //VarDeclarationLine// ``)`` ``:`` //Type// //VariableDeclaration// //Stmt2// 
  | //Type1// | -> | //Type2// 
  | //LitVal// | -> | //String// 
  |  |  **|**  | //Double// 
  |  |  **|**  | //Char// 


